.DS_Store
node_modules
custom
*.log

.versions
.idea
dist

# This repo uses pnpm (pnpm-lock.yaml) - ignore other package managers' lockfiles
# so an accidental `npm install`/`yarn` doesn't get committed.
package-lock.json
npm-shrinkwrap.json
yarn.lock


# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local
src/swiper-element-bundle.ts

# Agents
.claude
.agents
.cursor
.gemini
.codex
.opencode
